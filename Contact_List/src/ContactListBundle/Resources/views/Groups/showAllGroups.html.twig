{% extends '@ContactList/main.html.twig' %}
{% block content %}
    {% for group in groups %}
        <a href="{{ path('show_group_by_id', {"id" : group.id}) }}" class="myButton">{{ group.name }}, {{ group.description }}</a><br>
    {% endfor %}
{% endblock %}